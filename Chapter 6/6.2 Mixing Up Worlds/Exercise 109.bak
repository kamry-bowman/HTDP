;; The first three lines of this file were inserted by DrRacket. They record metadata
;; about the language level of this file in a form that our tools can easily process.
#reader(lib "htdp-beginner-reader.ss" "lang")((modname |Exercise 109|) (read-case-sensitive #t) (teachpacks ()) (htdp-settings #(#t constructor repeating-decimal #f #t none #f () #f)))
(require 2htdp/image)
(require 2htdp/universe)

(define WIDTH 100)
(define HEIGHT 100)

(define INITIAL-COLOR "white")
(define INITIAL-RECT (rectangle WIDTH HEIGHT "solid" INITIAL-COLOR))

(define FIRST-HIT-COLOR "yellow")
(define FIRST-HIT-RECT (rectangle WIDTH HEIGHT "solid" FIRST-HIT-COLOR))






(define (main ws)
  (big-bang ws
    [key-event process]